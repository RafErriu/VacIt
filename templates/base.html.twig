<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
       	<link rel="stylesheet" type="text/css" href="{{ asset('assets/bootstrap/css/bootstrap.css') }}"/>
		<link rel="stylesheet" type="text/css" href="{{ asset('assets/foundation/css/main.css') }}"/>  
        <link rel="stylesheet" type="text/css" href="{{ asset('assets/foundation/css/foundation.css') }}"/>
        <link rel="stylesheet" type="text/css" href="{{ asset('assets/foundation/css/foundation-float.css') }}"/>

        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
 
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>

    <div class="grid-x header">
        <div class="cell small-4">
            <a href="{{ path('homepage') }}">
            <img src="{{ asset('assets/images/vacit-logo-white.png')}}" class="logo">
            </a>
        </div>
        <div style="display: flex; justify-content: flex-end; align-items: flex-end;" class="cell small-8">
            <a href="{{ path('registreren') }}">
              <p class="registreren">REGISTREREN</p>
            </a>
            <a href="{{ path('inloggen') }}">
              <p class="inloggen">INLOGGEN</p>
            </a>
        
        </div>
     </div>
   
        {% block body %}{% endblock %}
    
     <div class="grid-x footer">
        <div class="cell small-4">
            <a href="{{ path('homepage') }}">
            <img src="{{ asset('assets/images/vacit-logo-white.png')}}" class="logo">
            </a>
        </div>
     </div>
    </body>
</html>
